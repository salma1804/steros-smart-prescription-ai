import { FlaskConical } from "lucide-react";
import PageEnConstruction from "../components/PageEnConstruction";

export default function Analyses() {
  return (
    <PageEnConstruction
      titre="Analyses"
      description="Suivi des analyses en cours"
      icone={FlaskConical}
    />
  );
}